<h1>Bookings index</h1>

<%#= link_to "Delete all rooms", delete_rooms_path(@bookings.first) %>
<%= link_to "Create a booking", new_booking_path %>

<% @bookings.each do |booking| %>
  <li>
    <%= link_to booking.video_url, booking_path(booking) %>
  </li>
<% end %>
